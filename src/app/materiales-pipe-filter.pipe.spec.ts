import { MaterialesPipeFilterPipe } from './materiales-pipe-filter.pipe';

describe('MaterialesPipeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MaterialesPipeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
